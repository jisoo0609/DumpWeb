<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dispatch.dump.commonModule.db.mapper.DailyReportSubMapper">
    <!--운행 정보 저장-->
    <insert id="insertDailyReportSub" parameterType="DailyReportSub">
        <!--idx 자동->따로 작성 X-->

        insert into
        tSheet_sub
        (fromsite, item, Qty, Qtyup, Rem, sheetID2, tosite)
        values
        (#{fromsite}, #{item}, #{Qty}, #{Qtyup}, #{Rem}, #{DailyReportMainMapper.sheetID}, #{tosite})

    </insert>
</mapper>