<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dispatch.dump.commonModule.db.mapper.DailyReportStep6Mapper">

    <select id="findCarListByDate" resultType="DailyReportStep6">
        SELECT DATE_FORMAT(drvdate,'%Y-%m-%d') AS 'drvdate',
               drvclub,
               lastkm,
               useamt,
               useoil,
               drvrem
        FROM   tdrive
        WHERE  carno = #{carNo}
        ORDER  BY drvdate DESC
    </select>
    
    <select id="findCarListByItem" resultType="DailyReportStep6">
        SELECT drvclub,
               DATE_FORMAT(drvdate,'%Y-%m-%d') AS 'drvdate',
               lastkm,
               useamt,
               useoil,
               drvrem
        FROM   tdrive
        WHERE  carno = #{carNo}
        AND drvclub = #{selectOption}
        ORDER  BY drvdate DESC
    </select>
</mapper>